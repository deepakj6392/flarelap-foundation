import { ReactNode } from "react";

export const metadata = {
  title: "Education Initiatives | Flarelap Global Foundation",
  description: "Providing quality education access, scholarships, digital literacy training, and after-school tutoring for underprivileged children.",
};

export default function EducationLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
